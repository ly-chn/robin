# 禁止lombok继续向上查找
# 记得同步所有lombok.config文件, 至于为什么不是import, 因为IDEA的代码提示不支持
config.stopbubbling = true
lombok.log.custom.declaration = org.apache.commons.logging.Log org.apache.commons.logging.LogFactory.getLog(TYPE)
